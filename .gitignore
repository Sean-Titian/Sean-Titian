# Local planning material for this workspace; never publish it with the profile.
.local/

# Private source material and isolated project repositories. Each public project
# is curated separately so institution materials and large datasets cannot leak
# into the profile repository.
Projects/
repos/

# Local resume review exports and helper scripts contain personal information
# and are not part of the public profile repository.
/_audit_comments.json
/_audit_resume_teacher/
/resume_current_review/
/resume_teacher_review/
/tools/

# Operating-system noise.
.DS_Store
Thumbs.db
Desktop.ini
